install_requires = ['Naked'],
install_requires = ['opencv']  # sudo apt-get install opencv-python
install_requires = ['numpy']  # pip install numpy
install_requires = ['mysqlclient']  # pip install mysqlclient
install_requires = ['pythoncom']  # pip install pypiwin32
install_requires = ['pyhook']  # in deps